services:
  postgres:
    image: postgres:16
    container_name: valoria-postgres
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - valoria-postgres-data:/var/lib/postgresql/data

volumes:
  valoria-postgres-data:
